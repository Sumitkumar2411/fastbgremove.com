---
interface Props {
  heading?: string;
  subheading?: string;
  defaultBg?: 'transparent' | 'white' | 'passport-blue';
}

const {
  heading = "Remove Background from Image",
  subheading = "100% automatic, in-browser background removal. No sign-up, no compression, and no watermarks.",
  defaultBg = "transparent"
} = Astro.props;
---

<section class="w-full flex flex-col items-center justify-center pt-2 sm:pt-4 pb-6" data-default-bg={defaultBg}>
  <!-- Desktop Hero Heading (Clean & Bold Cursor Typography) -->
  <div class="text-center max-w-3xl mx-auto mb-6 sm:mb-8 space-y-2">
    <h1 class="text-3xl sm:text-4xl md:text-5xl font-extrabold tracking-tight text-zinc-900 dark:text-white">
      {heading}
    </h1>
    <p class="text-base sm:text-lg text-zinc-600 dark:text-[#A7A7A7] max-w-2xl mx-auto leading-relaxed">
      {subheading}
    </p>
  </div>

  <!-- Main Workspace (Cursor Editor Grade Card with 8px/12px Geometry) -->
  <div id="workspace-card" class="w-full max-w-5xl bg-white dark:bg-[#1c1c1f] border border-[#e6e5e0] dark:border-[#2e2e33] rounded-lg shadow-xs p-3.5 sm:p-7 relative transition-all">
    <!-- Active Accent Corner Dot -->
    <div class="absolute -top-[1px] -left-[1px] w-2.5 h-2.5 rounded-tl-lg bg-[#E5A93C] z-20 pointer-events-none shadow-[0_0_8px_rgba(229,169,60,0.5)]"></div>

    <!-- STATE 1: UPLOAD DROPZONE VIEW (HERO ELEMENT) -->
    <div id="upload-view" class="flex flex-col items-center justify-center">
      <div
        id="drop-zone"
        tabindex="0"
        role="button"
        aria-label="Upload image for background removal"
        class="w-full min-h-[220px] sm:min-h-[340px] md:min-h-[440px] border-2 border-dashed border-[#cfcdc4] dark:border-[#2e2e33] hover:border-[#E5A93C] dark:hover:border-[#E5A93C] hover:bg-[#efeee8]/50 dark:hover:bg-[#1c1c1f] focus:border-[#D49B28] focus:outline-none rounded-lg p-4 sm:p-8 md:p-14 flex flex-col items-center justify-center text-center cursor-pointer transition-all group shadow-xs hover:shadow-[0_0_24px_rgba(229,169,60,0.15)] relative"
      >
        <input type="file" id="file-input" accept="image/png, image/jpeg, image/webp" class="hidden" />

        <!-- Large Upload Icon with Golden Accent -->
        <div class="w-16 h-16 rounded-md bg-[#E5A93C]/10 border border-[#E5A93C]/20 flex items-center justify-center text-[#E5A93C] group-hover:scale-105 transition-transform mb-6 shadow-xs">
          <svg class="w-8 h-8" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/>
            <polyline points="17 8 12 3 7 8"/>
            <line x1="12" y1="3" x2="12" y2="15"/>
          </svg>
        </div>

        <!-- Prominent Primary Callout -->
        <div class="space-y-2 mb-4">
          <p class="text-xl sm:text-2xl font-bold text-[#26251e] dark:text-white">
            Drop your image here, or <span class="text-[#b47818] dark:text-[#E5A93C] underline decoration-2 underline-offset-4 group-hover:text-[#D49B28]">Browse</span>
          </p>
          <p class="text-sm sm:text-base text-[#5a5852] dark:text-[#a09c92]">
            Supports PNG, JPG, WebP up to 25MB • Or paste directly with <kbd class="px-2 py-1 rounded-md bg-[#efeee8] dark:bg-[#242428] border border-[#cfcdc4] dark:border-[#3e3e44] text-xs font-mono text-[#26251e] dark:text-[#ededed] font-semibold">Ctrl+V</kbd>
          </p>
        </div>

        <div class="inline-flex items-center gap-2 px-3 py-1 rounded-md bg-[#E5A93C]/10 border border-[#E5A93C]/20 text-xs font-semibold text-[#b47818] dark:text-[#E5A93C]">
          <span class="w-2 h-2 rounded-full bg-[#E5A93C]"></span>
          <span>100% In-Browser & Private • Free Full Resolution</span>
        </div>
      </div>

      <!-- Sample Cards Row (Clickable 8px Thumbnail Cards) -->
      <div class="mt-4 sm:mt-6 flex flex-wrap items-center justify-center gap-2 sm:gap-3.5">
        <span class="text-xs sm:text-sm font-medium text-[#5a5852] dark:text-[#a09c92] w-full text-center sm:w-auto">Try with a sample:</span>

        <button
          type="button"
          data-sample="/samples/portrait.jpg"
          class="sample-btn flex items-center gap-2 sm:gap-3 p-1.5 pr-3 sm:p-2 sm:pr-4 rounded-md bg-[#f7f7f4] dark:bg-[#1c1c1f] border border-[#e6e5e0] dark:border-[#2e2e33] hover:border-[#E5A93C] hover:shadow-[0_0_12px_rgba(229,169,60,0.15)] transition-all cursor-pointer group/card"
        >
          <img src="/samples/portrait.jpg" alt="Portrait photo background removal sample" class="w-8 h-8 sm:w-10 sm:h-10 rounded-md object-cover group-hover/card:scale-105 transition-transform" />
          <div class="text-left">
            <div class="text-xs sm:text-sm font-semibold text-[#26251e] dark:text-[#ededed] group-hover/card:text-[#b47818] dark:group-hover/card:text-[#E5A93C]">Portrait</div>
            <div class="text-[10px] sm:text-xs text-[#807d72] dark:text-[#a09c92]">Headshot & People</div>
          </div>
        </button>

        <button
          type="button"
          data-sample="/samples/signature.jpg"
          class="sample-btn flex items-center gap-2 sm:gap-3 p-1.5 pr-3 sm:p-2 sm:pr-4 rounded-md bg-[#f7f7f4] dark:bg-[#1c1c1f] border border-[#e6e5e0] dark:border-[#2e2e33] hover:border-[#E5A93C] hover:shadow-[0_0_12px_rgba(229,169,60,0.15)] transition-all cursor-pointer group/card"
        >
          <img src="/samples/signature.jpg" alt="Handwritten signature transparency sample" class="w-8 h-8 sm:w-10 sm:h-10 rounded-md object-cover bg-white group-hover/card:scale-105 transition-transform" />
          <div class="text-left">
            <div class="text-xs sm:text-sm font-semibold text-[#26251e] dark:text-[#ededed] group-hover/card:text-[#b47818] dark:group-hover/card:text-[#E5A93C]">Signature</div>
            <div class="text-[10px] sm:text-xs text-[#807d72] dark:text-[#a09c92]">Ink & Documents</div>
          </div>
        </button>

        <button
          type="button"
          data-sample="/samples/product.jpg"
          class="sample-btn flex items-center gap-2 sm:gap-3 p-1.5 pr-3 sm:p-2 sm:pr-4 rounded-md bg-[#f7f7f4] dark:bg-[#1c1c1f] border border-[#e6e5e0] dark:border-[#2e2e33] hover:border-[#E5A93C] hover:shadow-[0_0_12px_rgba(229,169,60,0.15)] transition-all cursor-pointer group/card"
        >
          <img src="/samples/product.jpg" alt="E-commerce product background removal sample" class="w-8 h-8 sm:w-10 sm:h-10 rounded-md object-cover group-hover/card:scale-105 transition-transform" />
          <div class="text-left">
            <div class="text-xs sm:text-sm font-semibold text-[#26251e] dark:text-[#ededed] group-hover/card:text-[#b47818] dark:group-hover/card:text-[#E5A93C]">Product</div>
            <div class="text-[10px] sm:text-xs text-[#807d72] dark:text-[#a09c92]">Commercial Object</div>
          </div>
        </button>
      </div>
    </div>

    <!-- STATE 2: PROCESSING VIEW -->
    <div id="processing-view" class="hidden flex flex-col items-center justify-center min-h-[420px] p-8 text-center">
      <div class="w-12 h-12 rounded-full border-3 border-[#E5A93C]/20 border-t-[#E5A93C] animate-spin mb-5"></div>

      <div class="space-y-2 max-w-md w-full">
        <h3 id="processing-title" class="text-lg font-bold text-[#26251e] dark:text-white">
          Removing background locally...
        </h3>
        <p id="processing-subtitle" class="text-sm text-[#5a5852] dark:text-[#a09c92]">
          Running in-browser WebAssembly neural engine...
        </p>

        <!-- Progress Bar with Golden Accent Fill -->
        <div class="w-full bg-[#efeee8] dark:bg-[#121212] border border-[#e6e5e0] dark:border-[#2e2e33] rounded-full h-2 mt-4 overflow-hidden">
          <div id="progress-bar-fill" class="bg-[#E5A93C] h-full w-12 transition-all duration-200 rounded-full shadow-[0_0_10px_rgba(229,169,60,0.5)]"></div>
        </div>

        <p class="text-xs text-[#807d72] dark:text-[#706e68] pt-2">
          Model weights are cached locally. Your image is never uploaded to any server.
        </p>
      </div>

      <button
        id="cancel-processing-btn"
        type="button"
        class="mt-6 text-sm font-medium text-[#5a5852] hover:text-[#26251e] dark:text-[#a09c92] dark:hover:text-white underline underline-offset-4 cursor-pointer"
      >
        Cancel
      </button>
    </div>

    <!-- STATE 3: RESULT VIEW (INTERACTIVE BEFORE/AFTER SLIDER & INLINE CONTROLS) -->
    <div id="result-view" class="hidden flex flex-col space-y-3.5">
      <!-- Toolbar Header: Resolution badge & Ratio Segmented Controls -->
      <div class="flex flex-col sm:flex-row items-stretch sm:items-center justify-between gap-2.5 pb-2.5 border-b border-[#e6e5e0] dark:border-[#2e2e33]">
        <!-- Left: Image Resolution & Size Badge -->
        <div class="flex items-center justify-between sm:justify-start gap-2">
          <span class="inline-flex items-center gap-2 px-2.5 sm:px-3 py-1 sm:py-1.5 rounded-md bg-[#E5A93C]/10 border border-[#E5A93C]/20 text-[11px] sm:text-xs font-semibold text-[#b47818] dark:text-[#E5A93C] font-mono">
            <span class="w-2 h-2 rounded-full bg-[#E5A93C]"></span>
            <span id="meta-res-badge">HD READY</span>
          </span>
          <span id="meta-size-badge" class="text-xs font-medium text-[#807d72] dark:text-[#a09c92] font-mono hidden sm:inline-block"></span>
        </div>

        <!-- Right: Canvas Ratio Controls & 50% Split Reset -->
        <div class="flex items-center gap-1.5 overflow-x-auto no-scrollbar py-1 max-w-full">
          <div class="flex items-center gap-1.5 text-xs font-mono text-[#807d72] dark:text-[#a09c92] hidden md:flex shrink-0">
            <svg class="w-3.5 h-3.5 text-[#E5A93C]" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <rect x="3" y="3" width="18" height="18" rx="2" ry="2"/>
            </svg>
            <span>Ratio:</span>
          </div>

          <div id="ratio-selector" class="flex items-center p-1 rounded-md border border-[#e6e5e0] dark:border-[#2e2e33] bg-[#efeee8] dark:bg-[#121212] gap-1 shadow-xs text-xs font-mono shrink-0">
            <button
              type="button"
              data-ratio="original"
              class="ratio-btn active py-1.5 px-2.5 sm:px-3 rounded-md font-semibold text-[#b47818] dark:text-[#E5A93C] bg-white dark:bg-[#242428] shadow-xs cursor-pointer transition-all border border-[#E5A93C]/30 shrink-0 whitespace-nowrap"
            >
              Original
            </button>
            <button
              type="button"
              data-ratio="1:1"
              class="ratio-btn py-1.5 px-2.5 sm:px-3 rounded-md font-medium text-[#5a5852] dark:text-[#a09c92] hover:text-[#b47818] dark:hover:text-[#E5A93C] cursor-pointer transition-all border border-transparent shrink-0 whitespace-nowrap"
            >
              1:1
            </button>
            <button
              type="button"
              data-ratio="16:9"
              class="ratio-btn py-1.5 px-2.5 sm:px-3 rounded-md font-medium text-[#5a5852] dark:text-[#a09c92] hover:text-[#b47818] dark:hover:text-[#E5A93C] cursor-pointer transition-all border border-transparent shrink-0 whitespace-nowrap"
            >
              16:9
            </button>
            <button
              type="button"
              data-ratio="passport"
              class="ratio-btn py-1.5 px-2.5 sm:px-3 rounded-md font-medium text-[#5a5852] dark:text-[#a09c92] hover:text-[#b47818] dark:hover:text-[#E5A93C] cursor-pointer transition-all border border-transparent shrink-0 whitespace-nowrap"
            >
              Passport
            </button>
          </div>

          <button
            id="reset-slider-btn"
            type="button"
            title="Reset slider to 50% center"
            class="shrink-0 py-1.5 px-2.5 rounded-md border border-[#e6e5e0] dark:border-[#2e2e33] bg-white dark:bg-[#1c1c1f] text-xs font-mono font-medium text-[#5a5852] dark:text-[#a09c92] hover:text-[#b47818] dark:hover:text-[#E5A93C] hover:border-[#E5A93C] transition-all shadow-xs cursor-pointer whitespace-nowrap"
          >
            50%
          </button>
        </div>
      </div>

      <!-- INTERACTIVE BEFORE/AFTER PREVIEW SLIDER VIEWPORT -->
      <div
        id="preview-container"
        class="w-full relative min-h-[440px] sm:min-h-[500px] rounded-lg border border-[#e6e5e0] dark:border-[#2e2e33] flex items-center justify-center overflow-hidden bg-[#efeee8]/50 dark:bg-[#151518] transition-colors select-none p-3 sm:p-4"
      >
        <!-- Bounding Stage Box for Ratio Containment & Dragging -->
        <div
          id="stage-box"
          class="relative flex items-center justify-center transition-all overflow-hidden rounded-md max-h-[440px] sm:max-h-[480px] max-w-full select-none cursor-ew-resize touch-none shadow-xs border border-[#cfcdc4] dark:border-[#3e3e44]"
          style="--slider-pos: 50%; touch-action: none;"
        >
          <!-- LAYER 1: BASE (ORIGINAL UPLOADED IMAGE - LEFT SIDE) -->
          <div id="slider-base-layer" class="w-full h-full relative flex items-center justify-center overflow-hidden bg-[#f7f7f4] dark:bg-[#121212]">
            <img
              id="preview-original-img"
              alt="Original uploaded image preview"
              class="max-h-[420px] sm:max-h-[480px] w-auto max-w-full object-contain pointer-events-none select-none"
            />
            <!-- Left Badge: Original -->
            <span class="absolute top-3 left-3 text-xs font-mono font-semibold text-[#5a5852] dark:text-[#a09c92] bg-white/95 dark:bg-[#1c1c1f]/95 px-2.5 py-1 rounded-md border border-[#e6e5e0] dark:border-[#2e2e33] shadow-xs pointer-events-none z-20 backdrop-blur-xs">
              ORIGINAL
            </span>
          </div>

          <!-- LAYER 2: CLIPPED OVERLAY (CUTOUT + BACKDROP - RIGHT SIDE) -->
          <div
            id="slider-cutout-layer"
            class="absolute inset-0 z-10 overflow-hidden pointer-events-none flex items-center justify-center transition-none"
            style="clip-path: inset(0 0 0 var(--slider-pos, 50%));"
          >
            <!-- Background Layer (Checkerboard, Solid Color, or Custom BG Image + Blur) -->
            <div id="stage-bg-layer" class="absolute inset-0 z-0 bg-checkerboard transition-all overflow-hidden pointer-events-none flex items-center justify-center">
              <img id="custom-bg-preview-img" alt="Custom background image preview" class="hidden w-full h-full object-cover transition-all" />
              <!-- Hardware-Accelerated Blurred Background Layer (Original image scaled 1.04x to prevent blur edge-bleeding/halo) -->
              <img
                id="blurred-original-bg-img"
                alt="Blurred background preview"
                class="hidden max-h-[420px] sm:max-h-[480px] w-auto max-w-full object-contain pointer-events-none select-none transition-none"
                style="will-change: filter, transform; transform: scale(1.04) translateZ(0);"
              />
            </div>

            <!-- Foreground Cutout Image (Identical size & containment as original) -->
            <img
              id="preview-cutout-img"
              alt="Transparent background cutout preview"
              class="max-h-[420px] sm:max-h-[480px] w-auto max-w-full object-contain relative z-10 pointer-events-none select-none drop-shadow-xs transition-none"
              style="will-change: filter, transform; transform: translateZ(0);"
            />

            <!-- Right Badge: Cutout -->
            <span class="absolute top-3 right-3 text-xs font-mono font-semibold text-[#b47818] dark:text-[#E5A93C] bg-white/95 dark:bg-[#1c1c1f]/95 px-2.5 py-1 rounded-md border border-[#E5A93C]/20 shadow-xs pointer-events-none z-20 backdrop-blur-xs">
              CUTOUT
            </span>
          </div>

          <!-- CENTER DRAGGABLE DIVIDER -->
          <div
            id="slider-divider"
            class="absolute top-0 bottom-0 z-30 pointer-events-none flex items-center justify-center transition-none"
            style="left: var(--slider-pos, 50%); transform: translateX(-50%);"
          >
            <!-- Divider Vertical Bar (#E5A93C golden active accent) -->
            <div class="w-0.5 h-full bg-[#E5A93C] shadow-[0_0_10px_rgba(229,169,60,0.7)]"></div>

            <!-- Grab Handle Pill with Left/Right Arrows (↔) -->
            <div
              id="slider-handle"
              class="absolute w-9 h-9 sm:w-10 sm:h-10 rounded-full bg-white dark:bg-[#1c1c1f] border-2 border-[#E5A93C] shadow-[0_0_12px_rgba(229,169,60,0.35)] flex items-center justify-center text-[#b47818] dark:text-[#E5A93C] pointer-events-auto cursor-ew-resize hover:scale-110 active:scale-95 transition-transform"
              style="touch-action: none;"
              title="Drag left or right to inspect cutout contours (60fps)"
            >
              <svg class="w-4 h-4 text-[#b47818] dark:text-[#E5A93C]" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
                <polyline points="8 17 3 12 8 7"/>
                <polyline points="16 7 21 12 16 17"/>
              </svg>
            </div>
          </div>
        </div>
      </div>

      <!-- INLINE ACTION CONTROLS: ROW 1 (BACKDROP SELECTION & TABS) -->
      <div id="backdrop-panel" class="w-full p-3 sm:p-4 rounded-lg bg-white dark:bg-[#1c1c1f] border border-[#e6e5e0] dark:border-[#2e2e33] shadow-xs space-y-3">
        <!-- Tab Switcher Header -->
        <div class="flex items-center justify-between border-b border-[#e6e5e0]/80 dark:border-[#2e2e33]/80 pb-2.5">
          <div class="flex items-center gap-2">
            <span class="text-xs font-mono font-semibold uppercase tracking-wider text-[#26251e] dark:text-[#ededed] flex items-center gap-1.5">
              <svg class="w-4 h-4 text-[#E5A93C]" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <rect x="3" y="3" width="18" height="18" rx="2" ry="2"/>
                <circle cx="8.5" cy="8.5" r="1.5"/>
                <polyline points="21 15 16 10 5 21"/>
              </svg>
              <span>Backdrop</span>
            </span>
            <div id="bg-tab-group" class="flex items-center p-0.5 rounded-md border border-[#e6e5e0] dark:border-[#2e2e33] bg-[#efeee8] dark:bg-[#121212] gap-1 shadow-xs text-xs font-mono">
              <button
                type="button"
                id="bg-tab-btn-color"
                data-tab="color"
                class="bg-tab-btn active py-1 px-2.5 sm:px-3 rounded-md font-semibold text-[#b47818] dark:text-[#E5A93C] bg-white dark:bg-[#242428] shadow-xs cursor-pointer transition-all border border-[#E5A93C]/30 flex items-center gap-1.5 shrink-0"
              >
                <div class="w-2.5 h-2.5 rounded-full bg-gradient-to-tr from-blue-500 to-amber-500"></div>
                <span>Color</span>
              </button>
              <button
                type="button"
                id="bg-tab-btn-photo"
                data-tab="photo"
                class="bg-tab-btn py-1 px-2.5 sm:px-3 rounded-md font-medium text-[#5a5852] dark:text-[#a09c92] hover:text-[#b47818] dark:hover:text-[#E5A93C] cursor-pointer transition-all border border-transparent flex items-center gap-1.5 shrink-0"
              >
                <svg class="w-3 h-3 text-[#E5A93C]" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                  <rect x="3" y="3" width="18" height="18" rx="2" ry="2"/>
                  <circle cx="8.5" cy="8.5" r="1.5"/>
                  <polyline points="21 15 16 10 5 21"/>
                </svg>
                <span>Photo</span>
              </button>
              <button
                type="button"
                id="bg-tab-btn-magic"
                data-tab="magic"
                class="bg-tab-btn py-1 px-2.5 sm:px-3 rounded-md font-medium text-[#5a5852] dark:text-[#a09c92] hover:text-[#b47818] dark:hover:text-[#E5A93C] cursor-pointer transition-all border border-transparent flex items-center gap-1.5 shrink-0"
              >
                <svg class="w-3.5 h-3.5 text-[#E5A93C]" viewBox="0 0 24 24" fill="currentColor">
                  <path d="M12 2l2.4 7.2L22 12l-7.6 2.8L12 22l-2.4-7.2L2 12l7.6-2.8z"/>
                </svg>
                <span>Magic</span>
                <span class="text-[9px] px-1 py-0.2 rounded bg-[#E5A93C]/20 text-[#b47818] dark:text-[#E5A93C] font-bold uppercase tracking-wider">AI</span>
              </button>
            </div>
          </div>

          <span class="text-[11px] font-mono text-[#807d72] dark:text-[#a09c92] hidden md:inline">
            Drag ↔ slider to inspect edges live
          </span>
        </div>

        <!-- TAB 1: COLOR PRESETS -->
        <div id="bg-tab-content-color" class="bg-tab-content flex flex-wrap items-center gap-2">
          <div class="flex flex-wrap items-center p-1 rounded-md border border-[#e6e5e0] dark:border-[#2e2e33] bg-[#efeee8] dark:bg-[#121212] gap-1 shadow-xs max-w-full">
            <!-- Transparent Checkerboard Toggle -->
            <button
              id="bg-btn-transparent"
              type="button"
              title="Transparent Checkerboard"
              class="bg-color-btn active py-1.5 px-2.5 sm:px-3 rounded-md text-xs sm:text-sm font-semibold flex items-center gap-1.5 transition-all text-[#b47818] dark:text-[#E5A93C] bg-white dark:bg-[#242428] shadow-xs cursor-pointer border border-[#E5A93C]/30 shrink-0"
              data-bg="transparent"
            >
              <div class="w-3.5 h-3.5 rounded-sm bg-checkerboard-dense border border-zinc-300 dark:border-zinc-600"></div>
              <span>Transparent</span>
            </button>

            <!-- Pure White (#FFFFFF) -->
            <button
              id="bg-btn-white"
              type="button"
              title="Pure White (#FFFFFF)"
              class="bg-color-btn py-1.5 px-2.5 sm:px-3 rounded-md text-xs sm:text-sm font-medium flex items-center gap-1.5 transition-all text-[#5a5852] dark:text-[#a09c92] hover:text-[#b47818] dark:hover:text-[#E5A93C] cursor-pointer border border-transparent shrink-0"
              data-bg="#FFFFFF"
            >
              <div class="w-3.5 h-3.5 rounded-sm bg-white border border-zinc-300"></div>
              <span>White</span>
            </button>

            <!-- Royal / Passport Blue (#2563EB) -->
            <button
              id="bg-btn-blue"
              type="button"
              title="Royal / Passport Blue (#2563EB)"
              class="bg-color-btn py-1.5 px-2.5 sm:px-3 rounded-md text-xs sm:text-sm font-medium flex items-center gap-1.5 transition-all text-[#5a5852] dark:text-[#a09c92] hover:text-[#b47818] dark:hover:text-[#E5A93C] cursor-pointer border border-transparent shrink-0"
              data-bg="#2563EB"
            >
              <div class="w-3.5 h-3.5 rounded-sm bg-[#2563EB] border border-blue-400/40"></div>
              <span>Blue</span>
            </button>

            <!-- Custom Color Picker -->
            <label
              title="Custom Hex Color"
              class="relative flex items-center gap-1.5 py-1.5 px-2 sm:px-2.5 rounded-md text-xs sm:text-sm font-medium text-[#5a5852] dark:text-[#a09c92] hover:text-[#b47818] dark:hover:text-[#E5A93C] cursor-pointer border border-transparent shrink-0"
            >
              <input type="color" id="custom-color-picker" value="#000000" class="sr-only" />
              <div id="custom-color-swatch" class="w-3.5 h-3.5 rounded-sm border border-zinc-300 dark:border-zinc-600 bg-black"></div>
              <span>Custom Color</span>
            </label>
          </div>
        </div>

        <!-- TAB 2: PHOTO UPLOAD -->
        <div id="bg-tab-content-photo" class="bg-tab-content hidden flex flex-wrap items-center gap-3">
          <button
            id="bg-btn-custom-image"
            type="button"
            title="Upload Custom Backdrop Image"
            class="py-2 px-4 rounded-md border border-[#e6e5e0] dark:border-[#2e2e33] bg-[#f7f7f4] dark:bg-[#151518] hover:border-[#E5A93C] text-xs sm:text-sm font-medium text-[#26251e] dark:text-[#ededed] hover:text-[#b47818] dark:hover:text-[#E5A93C] transition-all flex items-center gap-2 cursor-pointer shadow-xs"
          >
            <svg class="w-4 h-4 text-[#E5A93C]" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <rect x="3" y="3" width="18" height="18" rx="2" ry="2"/>
              <circle cx="8.5" cy="8.5" r="1.5"/>
              <polyline points="21 15 16 10 5 21"/>
            </svg>
            <span>Choose Backdrop Photo</span>
          </button>
          <input type="file" id="custom-bg-input" accept="image/png, image/jpeg, image/webp" class="hidden" />

          <!-- Photo Thumbnail / Active Indicator -->
          <div id="photo-preview-wrapper" class="hidden flex items-center gap-2 p-1 pl-1.5 pr-2.5 rounded-md bg-[#efeee8] dark:bg-[#121212] border border-[#e6e5e0] dark:border-[#2e2e33] text-xs font-mono text-[#5a5852] dark:text-[#a09c92]">
            <img id="photo-preview-thumb" alt="Custom background thumbnail" class="w-6 h-6 rounded object-cover border border-[#cfcdc4] dark:border-[#3e3e44]" />
            <span id="photo-preview-name" class="truncate max-w-[120px] sm:max-w-[180px]">Photo applied</span>
            <button id="photo-remove-btn" type="button" title="Remove custom background" class="ml-1 text-xs text-red-500 hover:text-red-700 dark:hover:text-red-400 cursor-pointer">✕</button>
          </div>

          <span class="text-xs font-mono text-[#807d72] dark:text-[#706e68]">
            PNG, JPG, WebP up to 25MB
          </span>
        </div>

        <!-- TAB 3: MAGIC AI GENERATOR -->
        <div id="bg-tab-content-magic" class="bg-tab-content hidden space-y-2.5">
          <!-- Input & Generate CTA -->
          <div class="flex flex-col sm:flex-row items-stretch sm:items-center gap-2">
            <div class="relative flex-1">
              <div class="absolute inset-y-0 left-0 pl-3 flex items-center pointer-events-none text-[#E5A93C]">
                <svg class="w-4 h-4" viewBox="0 0 24 24" fill="currentColor">
                  <path d="M12 2l2.4 7.2L22 12l-7.6 2.8L12 22l-2.4-7.2L2 12l7.6-2.8z"/>
                </svg>
              </div>
              <input
                type="text"
                id="magic-prompt-input"
                placeholder="Describe your background (e.g. Modern executive office, sunset beach)"
                class="w-full h-10 pl-9 pr-3 text-xs sm:text-sm rounded-md bg-[#f7f7f4] dark:bg-[#151518] border border-[#e6e5e0] dark:border-[#2e2e33] text-[#26251e] dark:text-white placeholder-[#807d72] dark:placeholder-[#706e68] focus:outline-none focus:border-[#E5A93C] focus:ring-1 focus:ring-[#E5A93C] transition-all"
              />
            </div>
            <button
              type="button"
              id="magic-generate-btn"
              class="h-10 px-5 rounded-md bg-gradient-to-r from-[#E5A93C] to-[#D49B28] hover:from-[#D49B28] hover:to-[#c28c20] text-zinc-950 text-xs sm:text-sm font-bold transition-all flex items-center justify-center gap-2 shadow-[0_2px_10px_rgba(229,169,60,0.3)] hover:shadow-[0_4px_16px_rgba(229,169,60,0.45)] cursor-pointer disabled:opacity-50 disabled:cursor-not-allowed shrink-0 select-none active:scale-[0.99]"
            >
              <svg id="magic-btn-icon" class="w-4 h-4 shrink-0" viewBox="0 0 24 24" fill="currentColor">
                <path d="M12 2l2.4 7.2L22 12l-7.6 2.8L12 22l-2.4-7.2L2 12l7.6-2.8z"/>
              </svg>
              <span id="magic-btn-text">Generate</span>
              <!-- Golden Loading Spinner State -->
              <div id="magic-btn-spinner" class="hidden w-4 h-4 border-2 border-zinc-950/30 border-t-zinc-950 rounded-full animate-spin"></div>
            </button>
          </div>

          <!-- Quick-chip Suggestions -->
          <div class="flex flex-wrap items-center gap-1.5 pt-0.5">
            <span class="text-xs font-mono text-[#807d72] dark:text-[#a09c92]">Suggestions:</span>
            <button
              type="button"
              data-prompt="Studio Light, clean minimalist photography backdrop with soft warm lighting"
              class="magic-chip-btn text-[11px] font-medium px-2.5 py-1 rounded-md bg-[#f7f7f4] dark:bg-[#151518] border border-[#e6e5e0] dark:border-[#2e2e33] text-[#5a5852] dark:text-[#a09c92] hover:border-[#E5A93C] hover:text-[#b47818] dark:hover:text-[#E5A93C] hover:bg-[#E5A93C]/5 transition-all cursor-pointer"
            >
              Studio Light
            </button>
            <button
              type="button"
              data-prompt="Neon Street, futuristic cyberpunk night city with glowing neon reflections and bokeh"
              class="magic-chip-btn text-[11px] font-medium px-2.5 py-1 rounded-md bg-[#f7f7f4] dark:bg-[#151518] border border-[#e6e5e0] dark:border-[#2e2e33] text-[#5a5852] dark:text-[#a09c92] hover:border-[#E5A93C] hover:text-[#b47818] dark:hover:text-[#E5A93C] hover:bg-[#E5A93C]/5 transition-all cursor-pointer"
            >
              Neon Street
            </button>
            <button
              type="button"
              data-prompt="Clean Office, modern bright executive corner office with floor to ceiling glass windows"
              class="magic-chip-btn text-[11px] font-medium px-2.5 py-1 rounded-md bg-[#f7f7f4] dark:bg-[#151518] border border-[#e6e5e0] dark:border-[#2e2e33] text-[#5a5852] dark:text-[#a09c92] hover:border-[#E5A93C] hover:text-[#b47818] dark:hover:text-[#E5A93C] hover:bg-[#E5A93C]/5 transition-all cursor-pointer"
            >
              Clean Office
            </button>
            <button
              type="button"
              data-prompt="Nature Bokeh, lush green garden forest with golden sunset rays and soft depth of field"
              class="magic-chip-btn text-[11px] font-medium px-2.5 py-1 rounded-md bg-[#f7f7f4] dark:bg-[#151518] border border-[#e6e5e0] dark:border-[#2e2e33] text-[#5a5852] dark:text-[#a09c92] hover:border-[#E5A93C] hover:text-[#b47818] dark:hover:text-[#E5A93C] hover:bg-[#E5A93C]/5 transition-all cursor-pointer"
            >
              Nature Bokeh
            </button>
          </div>

          <!-- Inline Magic Status Box -->
          <div id="magic-status-box" class="hidden p-2.5 rounded-md text-xs font-medium flex items-center justify-between transition-all">
            <span id="magic-status-text"></span>
            <button type="button" id="magic-status-close" class="text-xs ml-2 opacity-70 hover:opacity-100 cursor-pointer">✕</button>
          </div>
        </div>
      </div>

      <!-- ADJUST & EFFECTS PANEL: REAL-TIME GPU-ACCELERATED BLUR & SHADOW CONTROLS -->
      <div id="effects-panel" class="w-full p-3 sm:p-4 rounded-lg bg-white dark:bg-[#1c1c1f] border border-[#e6e5e0] dark:border-[#2e2e33] shadow-xs space-y-3">
        <div class="flex items-center justify-between border-b border-[#e6e5e0]/80 dark:border-[#2e2e33]/80 pb-2">
          <div class="flex items-center gap-2">
            <svg class="w-4 h-4 text-[#E5A93C]" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <path d="M12 2v4M12 18v4M4.93 4.93l2.83 2.83M16.24 16.24l2.83 2.83M2 12h4M18 12h4M4.93 19.07l2.83-2.83M16.24 7.76l2.83-2.83"/>
            </svg>
            <span class="text-xs font-mono font-semibold uppercase tracking-wider text-[#26251e] dark:text-[#ededed]">Effects & Adjustments</span>
          </div>
          <span class="text-[11px] font-mono text-[#807d72] dark:text-[#a09c92]">60 FPS Live GPU</span>
        </div>

        <div class="grid grid-cols-1 md:grid-cols-2 gap-3 sm:gap-4">
          <!-- Effect 1: Blur Background -->
          <div class="p-3 rounded-md bg-[#f7f7f4] dark:bg-[#151518] border border-[#e6e5e0] dark:border-[#2e2e33] flex flex-col justify-between space-y-2.5">
            <div class="flex items-center justify-between">
              <span class="text-xs sm:text-sm font-semibold text-[#26251e] dark:text-[#ededed] flex items-center gap-1.5">
                <span>Blur background</span>
              </span>
              <!-- Toggle switch -->
              <label class="relative inline-flex items-center cursor-pointer select-none">
                <input type="checkbox" id="blur-bg-toggle" class="sr-only peer" />
                <div class="w-8 h-4.5 sm:w-9 sm:h-5 bg-zinc-300 dark:bg-zinc-700 peer-focus:outline-none rounded-full peer peer-checked:after:translate-x-full peer-checked:after:border-white after:content-[''] after:absolute after:top-[2px] after:left-[2px] after:bg-white after:border-zinc-300 after:border after:rounded-full after:h-3.5 after:w-3.5 sm:after:h-4 sm:after:w-4 after:transition-all peer-checked:bg-[#E5A93C]"></div>
              </label>
            </div>

            <div class="flex items-center gap-3 pt-0.5">
              <input
                type="range"
                id="blur-bg-slider"
                min="0"
                max="30"
                value="0"
                step="1"
                disabled
                class="flex-1 h-1.5 accent-[#E5A93C] cursor-pointer opacity-50 disabled:cursor-not-allowed transition-opacity"
                title="Adjust background blur (0-30px)"
              />
              <span id="blur-bg-label" class="w-10 text-right text-xs font-mono font-semibold text-[#807d72] dark:text-[#a09c92]">0px</span>
            </div>
          </div>

          <!-- Effect 2: Add Shadow / Opacity -->
          <div class="p-3 rounded-md bg-[#f7f7f4] dark:bg-[#151518] border border-[#e6e5e0] dark:border-[#2e2e33] flex flex-col justify-between space-y-2.5">
            <div class="flex items-center justify-between">
              <span class="text-xs sm:text-sm font-semibold text-[#26251e] dark:text-[#ededed] flex items-center gap-1.5">
                <span>Add Shadow / Opacity</span>
              </span>
              <!-- Toggle switch -->
              <label class="relative inline-flex items-center cursor-pointer select-none">
                <input type="checkbox" id="shadow-toggle" class="sr-only peer" />
                <div class="w-8 h-4.5 sm:w-9 sm:h-5 bg-zinc-300 dark:bg-zinc-700 peer-focus:outline-none rounded-full peer peer-checked:after:translate-x-full peer-checked:after:border-white after:content-[''] after:absolute after:top-[2px] after:left-[2px] after:bg-white after:border-zinc-300 after:border after:rounded-full after:h-3.5 after:w-3.5 sm:after:h-4 sm:after:w-4 after:transition-all peer-checked:bg-[#E5A93C]"></div>
              </label>
            </div>

            <div class="flex items-center gap-3 pt-0.5">
              <input
                type="range"
                id="shadow-slider"
                min="0"
                max="100"
                value="0"
                step="5"
                disabled
                class="flex-1 h-1.5 accent-[#E5A93C] cursor-pointer opacity-50 disabled:cursor-not-allowed transition-opacity"
                title="Adjust shadow depth & opacity (0-100%)"
              />
              <span id="shadow-label" class="w-10 text-right text-xs font-mono font-semibold text-[#807d72] dark:text-[#a09c92]">0%</span>
            </div>
          </div>
        </div>
      </div>

      <!-- Action Bar: Prominent Primary Button & Secondary Actions -->
      <div class="flex flex-col sm:flex-row items-center justify-between gap-3 pt-2">
        <!-- Right Action Group (Shown First on Mobile via order-1 sm:order-2) -->
        <div class="w-full sm:w-auto flex flex-col sm:flex-row items-stretch sm:items-center gap-2.5 order-1 sm:order-2">
          <!-- Primary Hero Button: Download Full HD PNG (Full width on mobile for easy one-tap export, WCAG AAA compliant text) -->
          <button
            id="download-hd-btn"
            type="button"
            class="w-full sm:w-auto h-12 sm:h-11 px-7 rounded-md bg-[#E5A93C] hover:bg-[#D49B28] active:bg-[#c28c20] text-zinc-950 text-sm sm:text-base font-bold transition-all flex items-center justify-center gap-2.5 shadow-[0_2px_12px_rgba(229,169,60,0.35)] hover:shadow-[0_4px_20px_rgba(229,169,60,0.5)] cursor-pointer active:scale-[0.99] order-1 sm:order-3"
          >
            <svg class="w-5 h-5 text-zinc-950 shrink-0" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2">
              <path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/>
              <polyline points="7 10 12 15 17 10"/>
              <line x1="12" y1="15" x2="12" y2="3"/>
            </svg>
            <span class="whitespace-nowrap">Download Full HD PNG</span>
          </button>

          <!-- Secondary Actions: Clean 2-Column Grid on Mobile, Inline on Desktop -->
          <div class="w-full sm:w-auto grid grid-cols-2 sm:flex items-center gap-2 sm:gap-2.5 order-2 sm:order-1">
            <!-- Copy to Clipboard Button -->
            <button
              id="copy-clipboard-btn"
              type="button"
              class="w-full sm:w-auto h-11 px-3 sm:px-4 rounded-md bg-white dark:bg-[#1c1c1f] border border-[#e6e5e0] dark:border-[#2e2e33] hover:border-[#E5A93C] text-xs sm:text-sm font-medium text-[#26251e] dark:text-[#ededed] hover:text-[#b47818] dark:hover:text-[#E5A93C] transition-all flex items-center justify-center gap-1.5 sm:gap-2 cursor-pointer shadow-xs"
              title="Copy cutout image to clipboard"
            >
              <svg class="w-4 h-4 text-[#E5A93C] shrink-0" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <rect x="9" y="9" width="13" height="13" rx="2" ry="2"/>
                <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1"/>
              </svg>
              <span id="copy-btn-text" class="truncate">Copy PNG</span>
            </button>

            <!-- DUAL EXPORT TRIGGER: Compressed JPG with discrete target selector -->
            <div class="w-full sm:w-auto flex items-center shadow-xs">
              <select
                id="jpg-preset-select"
                aria-label="JPG compression target"
                class="w-1/2 sm:w-auto h-11 pl-2 sm:pl-3 pr-1 sm:pr-2 rounded-l-md border border-r-0 border-[#e6e5e0] dark:border-[#2e2e33] bg-[#f7f7f4] dark:bg-[#1c1c1f] text-[11px] sm:text-xs font-mono font-medium text-[#26251e] dark:text-[#ededed] cursor-pointer focus:outline-none focus:border-[#E5A93C]"
              >
                <option value="50kb">&lt;50KB</option>
                <option value="100kb" selected>&lt;100KB</option>
                <option value="max">Max</option>
              </select>
              <button
                id="export-jpg-btn"
                type="button"
                class="w-1/2 sm:w-auto h-11 px-2 sm:px-4 rounded-r-md border border-[#e6e5e0] dark:border-[#2e2e33] bg-white dark:bg-[#1c1c1f] hover:border-[#E5A93C] text-[11px] sm:text-xs md:text-sm font-medium text-[#26251e] dark:text-[#ededed] hover:text-[#b47818] dark:hover:text-[#E5A93C] transition-all flex items-center justify-center gap-1 sm:gap-1.5 cursor-pointer whitespace-nowrap"
                title="Export compressed JPG strictly below target threshold"
              >
                <svg class="w-3.5 sm:w-4 h-3.5 sm:h-4 text-[#E5A93C] shrink-0" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                  <path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/>
                  <polyline points="7 10 12 15 17 10"/>
                  <line x1="12" y1="15" x2="12" y2="3"/>
                </svg>
                <span id="export-jpg-text">JPG</span>
              </button>
            </div>
          </div>
        </div>

        <!-- Left: Upload Another (Order 2 on mobile so Download HD is top priority) -->
        <button
          id="upload-another-btn"
          type="button"
          class="w-full sm:w-auto h-11 px-5 rounded-md bg-[#f7f7f4] dark:bg-[#1c1c1f] border border-[#e6e5e0] dark:border-[#2e2e33] hover:border-[#cfcdc4] dark:hover:border-[#3e3e44] text-xs sm:text-sm font-medium text-[#26251e] dark:text-[#ededed] transition-all flex items-center justify-center gap-2 cursor-pointer shadow-xs order-2 sm:order-1"
        >
          <svg class="w-4 h-4 text-[#807d72] dark:text-[#a09c92]" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/>
            <polyline points="17 8 12 3 7 8"/>
            <line x1="12" y1="3" x2="12" y2="15"/>
          </svg>
          <span>Select New Image</span>
        </button>
      </div>
    </div>

    <!-- Error Toast Box -->
    <div id="error-box" class="hidden mt-4 p-3 rounded-md bg-red-50 dark:bg-red-950/40 border border-red-200 dark:border-red-800/60 text-red-700 dark:text-red-200 text-sm font-medium flex items-center justify-between">
      <span id="error-message">Processing error.</span>
      <button id="close-error-btn" type="button" class="text-red-500 hover:text-red-700 dark:hover:text-red-200 ml-3">✕</button>
    </div>
  </div>

  <!-- Toast Notification -->
  <div id="paste-toast" class="fixed bottom-6 right-6 z-50 transform translate-y-12 opacity-0 pointer-events-none transition-all duration-200 bg-white dark:bg-[#1c1c1f] border border-[#e6e5e0] dark:border-[#2e2e33] text-[#26251e] dark:text-white px-4 py-2.5 rounded-md shadow-lg flex items-center gap-2.5 text-sm font-medium">
    <span class="w-2 h-2 rounded-full bg-[#E5A93C]"></span>
    <span id="paste-toast-text">Pasted image from clipboard</span>
  </div>
</section>

<script>
  import {
    createHdExportBlob,
    createCompressedJpgBlob,
    refineAlphaMask,
    triggerDownload,
    copyImageToClipboard,
    type CanvasRatio,
    type JpgCompressionPreset,
    type ExportOptions
  } from '../lib/canvasExport';

  // Global state
  let currentFile: File | Blob | null = null;
  let originalImageUrl: string | null = null;
  let cutoutBlob: Blob | null = null;
  let cutoutUrl: string | null = null;
  let currentBgColor: string = 'transparent';
  let customBgImageObj: HTMLImageElement | null = null;
  let customBgObjectUrl: string | null = null;
  let currentBlur: number = 0;
  let currentRatio: CanvasRatio = 'original';
  let imageNaturalWidth: number = 0;
  let imageNaturalHeight: number = 0;
  let isCancelled: boolean = false;

  // DOM Elements
  const sectionRoot = document.querySelector('section[data-default-bg]') as HTMLElement;
  const initialDefaultBg = sectionRoot?.dataset.defaultBg || 'transparent';

  const dropZone = document.getElementById('drop-zone') as HTMLElement;
  const fileInput = document.getElementById('file-input') as HTMLInputElement;
  const uploadView = document.getElementById('upload-view') as HTMLElement;
  const processingView = document.getElementById('processing-view') as HTMLElement;
  const resultView = document.getElementById('result-view') as HTMLElement;
  const errorBox = document.getElementById('error-box') as HTMLElement;
  const errorMessage = document.getElementById('error-message') as HTMLElement;
  const closeErrorBtn = document.getElementById('close-error-btn') as HTMLElement;
  const pasteToast = document.getElementById('paste-toast') as HTMLElement;
  const pasteToastText = document.getElementById('paste-toast-text') as HTMLElement;

  const processingTitle = document.getElementById('processing-title') as HTMLElement;
  const processingSubtitle = document.getElementById('processing-subtitle') as HTMLElement;
  const progressBarFill = document.getElementById('progress-bar-fill') as HTMLElement;
  const cancelProcessingBtn = document.getElementById('cancel-processing-btn') as HTMLElement;

  const previewContainer = document.getElementById('preview-container') as HTMLElement;
  const stageBox = document.getElementById('stage-box') as HTMLElement;
  const stageBgLayer = document.getElementById('stage-bg-layer') as HTMLElement;
  const customBgPreviewImg = document.getElementById('custom-bg-preview-img') as HTMLImageElement;
  const previewCutoutImg = document.getElementById('preview-cutout-img') as HTMLImageElement;
  const previewOriginalImg = document.getElementById('preview-original-img') as HTMLImageElement;
  const sliderDivider = document.getElementById('slider-divider') as HTMLElement;
  const sliderHandle = document.getElementById('slider-handle') as HTMLElement;
  const resetSliderBtn = document.getElementById('reset-slider-btn') as HTMLButtonElement;

  const metaResBadge = document.getElementById('meta-res-badge') as HTMLElement;
  const metaSizeBadge = document.getElementById('meta-size-badge') as HTMLElement;

  const bgBtnTransparent = document.getElementById('bg-btn-transparent') as HTMLButtonElement;
  const bgBtnWhite = document.getElementById('bg-btn-white') as HTMLButtonElement;
  const bgBtnBlue = document.getElementById('bg-btn-blue') as HTMLButtonElement;
  const customColorPicker = document.getElementById('custom-color-picker') as HTMLInputElement;
  const customColorSwatch = document.getElementById('custom-color-swatch') as HTMLElement;
  const bgBtnCustomImage = document.getElementById('bg-btn-custom-image') as HTMLButtonElement;
  const customBgInput = document.getElementById('custom-bg-input') as HTMLInputElement;

  // Backdrop Tabs & Magic AI Elements
  const bgTabBtns = document.querySelectorAll('.bg-tab-btn');
  const bgTabContents = document.querySelectorAll('.bg-tab-content');
  const bgTabBtnColor = document.getElementById('bg-tab-btn-color') as HTMLButtonElement;
  const bgTabBtnPhoto = document.getElementById('bg-tab-btn-photo') as HTMLButtonElement;
  const bgTabBtnMagic = document.getElementById('bg-tab-btn-magic') as HTMLButtonElement;
  const bgTabContentColor = document.getElementById('bg-tab-content-color') as HTMLElement;
  const bgTabContentPhoto = document.getElementById('bg-tab-content-photo') as HTMLElement;
  const bgTabContentMagic = document.getElementById('bg-tab-content-magic') as HTMLElement;

  const magicPromptInput = document.getElementById('magic-prompt-input') as HTMLInputElement;
  const magicGenerateBtn = document.getElementById('magic-generate-btn') as HTMLButtonElement;
  const magicBtnSpinner = document.getElementById('magic-btn-spinner') as HTMLElement;
  const magicBtnIcon = document.getElementById('magic-btn-icon') as HTMLElement;
  const magicBtnText = document.getElementById('magic-btn-text') as HTMLElement;
  const magicChips = document.querySelectorAll('.magic-chip-btn');
  const magicStatusBox = document.getElementById('magic-status-box') as HTMLElement;
  const magicStatusText = document.getElementById('magic-status-text') as HTMLElement;
  const magicStatusClose = document.getElementById('magic-status-close') as HTMLElement;

  const photoPreviewWrapper = document.getElementById('photo-preview-wrapper') as HTMLElement;
  const photoPreviewThumb = document.getElementById('photo-preview-thumb') as HTMLImageElement;
  const photoPreviewName = document.getElementById('photo-preview-name') as HTMLElement;
  const photoRemoveBtn = document.getElementById('photo-remove-btn') as HTMLButtonElement;

  let currentBgTab: 'color' | 'photo' | 'magic' = 'color';

  const blurControlWrapper = document.getElementById('blur-control-wrapper') as HTMLElement;
  const backdropBlurSlider = document.getElementById('backdrop-blur-slider') as HTMLInputElement;
  const blurValueLabel = document.getElementById('blur-value-label') as HTMLElement;

  // New GPU-Accelerated Effects Elements & State
  const blurredOriginalBgImg = document.getElementById('blurred-original-bg-img') as HTMLImageElement;
  const blurBgToggle = document.getElementById('blur-bg-toggle') as HTMLInputElement;
  const blurBgSlider = document.getElementById('blur-bg-slider') as HTMLInputElement;
  const blurBgLabel = document.getElementById('blur-bg-label') as HTMLElement;
  const shadowToggle = document.getElementById('shadow-toggle') as HTMLInputElement;
  const shadowSlider = document.getElementById('shadow-slider') as HTMLInputElement;
  const shadowLabel = document.getElementById('shadow-label') as HTMLElement;

  let isBlurBgActive: boolean = false;
  let blurBgPx: number = 0;
  let isShadowActive: boolean = false;
  let shadowOpacityPct: number = 0;
  let originalImageObj: HTMLImageElement | null = null;

  const ratioButtons = document.querySelectorAll('.ratio-btn');

  const uploadAnotherBtn = document.getElementById('upload-another-btn') as HTMLButtonElement;
  const copyClipboardBtn = document.getElementById('copy-clipboard-btn') as HTMLButtonElement;
  const copyBtnText = document.getElementById('copy-btn-text') as HTMLElement;
  const downloadHdBtn = document.getElementById('download-hd-btn') as HTMLButtonElement;
  const exportJpgBtn = document.getElementById('export-jpg-btn') as HTMLButtonElement;
  const exportJpgText = document.getElementById('export-jpg-text') as HTMLElement;
  const jpgPresetSelect = document.getElementById('jpg-preset-select') as HTMLInputElement;

  // Setup initial color mode based on page prop
  if (initialDefaultBg === 'white') {
    currentBgColor = '#FFFFFF';
  } else if (initialDefaultBg === 'passport-blue') {
    currentBgColor = '#0055A5';
  } else {
    currentBgColor = 'transparent';
  }

  function updateResolutionBadge() {
    if (!metaResBadge) return;
    if (currentRatio === 'original') {
      if (imageNaturalWidth && imageNaturalHeight) {
        metaResBadge.textContent = `${imageNaturalWidth} × ${imageNaturalHeight} PX HD`;
      } else {
        metaResBadge.textContent = 'FULL RESOLUTION HD';
      }
    } else if (currentRatio === '1:1') {
      const side = Math.max(imageNaturalWidth, imageNaturalHeight);
      metaResBadge.textContent = `1:1 • ${side} × ${side} PX HD`;
    } else if (currentRatio === '16:9') {
      metaResBadge.textContent = '16:9 (YOUTUBE) HD';
    } else if (currentRatio === 'passport') {
      metaResBadge.textContent = 'PASSPORT 3.5×4.5 CM';
    }
  }

  function setCanvasRatio(ratio: CanvasRatio) {
    currentRatio = ratio;
    const activeClasses = ['active', 'bg-white', 'dark:bg-[#242428]', 'text-[#b47818]', 'dark:text-[#E5A93C]', 'font-semibold', 'shadow-xs', 'border-[#E5A93C]/30'];
    const inactiveClasses = ['text-[#5a5852]', 'dark:text-[#a09c92]', 'border-transparent'];

    ratioButtons.forEach(btn => {
      if (btn.getAttribute('data-ratio') === ratio) {
        btn.classList.add(...activeClasses);
        btn.classList.remove(...inactiveClasses);
      } else {
        btn.classList.remove(...activeClasses);
        btn.classList.add(...inactiveClasses);
      }
    });

    if (!stageBox) return;

    if (ratio === 'original') {
      stageBox.style.aspectRatio = '';
      stageBox.style.width = 'auto';
      stageBox.style.height = 'auto';
      stageBox.classList.remove('border', 'border-[#cfcdc4]', 'dark:border-[#3e3e44]', 'shadow-xs');
      previewCutoutImg.style.width = 'auto';
      previewCutoutImg.style.height = 'auto';
      if (previewOriginalImg) {
        previewOriginalImg.style.width = 'auto';
        previewOriginalImg.style.height = 'auto';
      }
      if (blurredOriginalBgImg) {
        blurredOriginalBgImg.style.width = 'auto';
        blurredOriginalBgImg.style.height = 'auto';
      }
    } else if (ratio === '1:1') {
      stageBox.style.aspectRatio = '1 / 1';
      stageBox.style.width = 'min(100%, 460px)';
      stageBox.style.height = 'min(100%, 460px)';
      stageBox.classList.add('border', 'border-[#cfcdc4]', 'dark:border-[#3e3e44]', 'shadow-xs');
      previewCutoutImg.style.width = '100%';
      previewCutoutImg.style.height = '100%';
      if (previewOriginalImg) {
        previewOriginalImg.style.width = '100%';
        previewOriginalImg.style.height = '100%';
      }
      if (blurredOriginalBgImg) {
        blurredOriginalBgImg.style.width = '100%';
        blurredOriginalBgImg.style.height = '100%';
      }
    } else if (ratio === '16:9') {
      stageBox.style.aspectRatio = '16 / 9';
      stageBox.style.width = 'min(100%, 680px)';
      stageBox.style.height = 'auto';
      stageBox.classList.add('border', 'border-[#cfcdc4]', 'dark:border-[#3e3e44]', 'shadow-xs');
      previewCutoutImg.style.width = '100%';
      previewCutoutImg.style.height = '100%';
      if (previewOriginalImg) {
        previewOriginalImg.style.width = '100%';
        previewOriginalImg.style.height = '100%';
      }
      if (blurredOriginalBgImg) {
        blurredOriginalBgImg.style.width = '100%';
        blurredOriginalBgImg.style.height = '100%';
      }
    } else if (ratio === 'passport') {
      stageBox.style.aspectRatio = '3.5 / 4.5';
      stageBox.style.width = 'min(100%, 350px)';
      stageBox.style.height = 'min(100%, 450px)';
      stageBox.classList.add('border', 'border-[#cfcdc4]', 'dark:border-[#3e3e44]', 'shadow-xs');
      previewCutoutImg.style.width = '100%';
      previewCutoutImg.style.height = '100%';
      if (previewOriginalImg) {
        previewOriginalImg.style.width = '100%';
        previewOriginalImg.style.height = '100%';
      }
      if (blurredOriginalBgImg) {
        blurredOriginalBgImg.style.width = '100%';
        blurredOriginalBgImg.style.height = '100%';
      }
    }

    updateResolutionBadge();
  }

  ratioButtons.forEach(btn => {
    btn.addEventListener('click', () => {
      const r = btn.getAttribute('data-ratio') as CanvasRatio;
      if (r) setCanvasRatio(r);
    });
  });

  function clearCustomBg() {
    customBgImageObj = null;
    if (customBgObjectUrl) {
      URL.revokeObjectURL(customBgObjectUrl);
      customBgObjectUrl = null;
    }
    if (customBgPreviewImg) {
      customBgPreviewImg.classList.add('hidden');
      customBgPreviewImg.src = '';
      customBgPreviewImg.style.transform = 'none';
      customBgPreviewImg.style.filter = 'none';
    }
    if (customBgInput) customBgInput.value = '';
    if (photoPreviewWrapper) photoPreviewWrapper.classList.add('hidden');
    if (photoPreviewThumb) photoPreviewThumb.src = '';
  }

  function applyBgToElements(color: string) {
    const isTrans = color === 'transparent' && !customBgImageObj;
    const isWhite = color.toUpperCase() === '#FFFFFF';
    const isBlue = color.toUpperCase() === '#0055A5' || color.toUpperCase() === '#2563EB';
    const isCustomImg = !!customBgImageObj;

    // Update buttons
    const activeClasses = ['active', 'bg-white', 'dark:bg-[#242428]', 'text-[#b47818]', 'dark:text-[#E5A93C]', 'font-semibold', 'shadow-xs', 'border-[#E5A93C]/30'];
    const inactiveClasses = ['text-[#5a5852]', 'dark:text-[#a09c92]', 'border-transparent'];

    [bgBtnTransparent, bgBtnWhite, bgBtnBlue, bgBtnCustomImage].forEach(b => {
      if (b) {
        b.classList.remove(...activeClasses);
        b.classList.add(...inactiveClasses);
      }
    });

    if (isCustomImg && bgBtnCustomImage) {
      bgBtnCustomImage.classList.add(...activeClasses);
      bgBtnCustomImage.classList.remove(...inactiveClasses);
    } else if (isTrans && bgBtnTransparent) {
      bgBtnTransparent.classList.add(...activeClasses);
      bgBtnTransparent.classList.remove(...inactiveClasses);
    } else if (isWhite && bgBtnWhite) {
      bgBtnWhite.classList.add(...activeClasses);
      bgBtnWhite.classList.remove(...inactiveClasses);
    } else if (isBlue && bgBtnBlue) {
      bgBtnBlue.classList.add(...activeClasses);
      bgBtnBlue.classList.remove(...inactiveClasses);
    }

    // Apply to preview backdrop layers
    if (stageBgLayer) {
      if (isCustomImg) {
        stageBgLayer.style.backgroundColor = 'transparent';
        stageBgLayer.classList.remove('bg-checkerboard');
      } else if (isTrans && !isBlurBgActive) {
        stageBgLayer.style.backgroundColor = '';
        stageBgLayer.classList.add('bg-checkerboard');
      } else {
        stageBgLayer.style.backgroundColor = color;
        stageBgLayer.classList.remove('bg-checkerboard');
        if (customColorSwatch) customColorSwatch.style.backgroundColor = color;
      }
    }
  }

  // Real-time GPU Blur Effect Handler
  function applyBlurEffect() {
    if (!blurredOriginalBgImg) return;
    if (isBlurBgActive && blurBgPx > 0) {
      blurredOriginalBgImg.classList.remove('hidden');
      blurredOriginalBgImg.style.filter = `blur(${blurBgPx}px)`;
      blurredOriginalBgImg.style.transform = 'scale(1.04) translateZ(0)';
      blurredOriginalBgImg.style.willChange = 'filter, transform';
      if (stageBgLayer) stageBgLayer.classList.remove('bg-checkerboard');
      if (customBgPreviewImg && customBgImageObj) {
        customBgPreviewImg.style.filter = `blur(${blurBgPx}px)`;
        customBgPreviewImg.style.transform = 'scale(1.04) translateZ(0)';
        customBgPreviewImg.style.willChange = 'filter, transform';
      }
    } else {
      blurredOriginalBgImg.classList.add('hidden');
      blurredOriginalBgImg.style.filter = 'none';
      if (customBgPreviewImg) {
        customBgPreviewImg.style.filter = 'none';
        customBgPreviewImg.style.transform = 'none';
      }
      if (currentBgColor === 'transparent' && !customBgImageObj && stageBgLayer) {
        stageBgLayer.classList.add('bg-checkerboard');
      }
    }
  }

  // Real-time GPU Drop Shadow Effect Handler
  function applyShadowEffect() {
    if (!previewCutoutImg) return;
    if (isShadowActive && shadowOpacityPct > 0) {
      const factor = shadowOpacityPct / 100;
      const blur = Math.round(18 * factor);
      const offsetY = Math.round(10 * factor);
      const alpha = (0.65 * factor).toFixed(2);
      previewCutoutImg.style.filter = `drop-shadow(0px ${offsetY}px ${blur}px rgba(0, 0, 0, ${alpha}))`;
      previewCutoutImg.style.transform = 'translateZ(0)';
      previewCutoutImg.style.willChange = 'filter, transform';
    } else {
      previewCutoutImg.style.filter = 'none';
      previewCutoutImg.style.transform = 'translateZ(0)';
    }
  }

  // Blur Background Switch & Slider Listeners
  if (blurBgToggle) {
    blurBgToggle.addEventListener('change', () => {
      isBlurBgActive = blurBgToggle.checked;
      if (isBlurBgActive) {
        if (blurBgSlider) {
          blurBgSlider.disabled = false;
          blurBgSlider.classList.remove('opacity-50', 'disabled:cursor-not-allowed');
          if (blurBgPx === 0) {
            blurBgPx = 12;
            blurBgSlider.value = '12';
            if (blurBgLabel) blurBgLabel.textContent = '12px';
          }
        }
      } else {
        if (blurBgSlider) {
          blurBgSlider.disabled = true;
          blurBgSlider.classList.add('opacity-50', 'disabled:cursor-not-allowed');
        }
      }
      applyBlurEffect();
    });
  }

  if (blurBgSlider) {
    blurBgSlider.addEventListener('input', () => {
      blurBgPx = parseInt(blurBgSlider.value, 10) || 0;
      if (blurBgLabel) blurBgLabel.textContent = `${blurBgPx}px`;
      if (blurBgPx > 0 && blurBgToggle && !blurBgToggle.checked) {
        blurBgToggle.checked = true;
        isBlurBgActive = true;
        blurBgSlider.disabled = false;
        blurBgSlider.classList.remove('opacity-50', 'disabled:cursor-not-allowed');
      }
      applyBlurEffect();
    });
  }

  // Add Shadow / Opacity Switch & Slider Listeners
  if (shadowToggle) {
    shadowToggle.addEventListener('change', () => {
      isShadowActive = shadowToggle.checked;
      if (isShadowActive) {
        if (shadowSlider) {
          shadowSlider.disabled = false;
          shadowSlider.classList.remove('opacity-50', 'disabled:cursor-not-allowed');
          if (shadowOpacityPct === 0) {
            shadowOpacityPct = 60;
            shadowSlider.value = '60';
            if (shadowLabel) shadowLabel.textContent = '60%';
          }
        }
      } else {
        if (shadowSlider) {
          shadowSlider.disabled = true;
          shadowSlider.classList.add('opacity-50', 'disabled:cursor-not-allowed');
        }
      }
      applyShadowEffect();
    });
  }

  if (shadowSlider) {
    shadowSlider.addEventListener('input', () => {
      shadowOpacityPct = parseInt(shadowSlider.value, 10) || 0;
      if (shadowLabel) shadowLabel.textContent = `${shadowOpacityPct}%`;
      if (shadowOpacityPct > 0 && shadowToggle && !shadowToggle.checked) {
        shadowToggle.checked = true;
        isShadowActive = true;
        shadowSlider.disabled = false;
        shadowSlider.classList.remove('opacity-50', 'disabled:cursor-not-allowed');
      }
      applyShadowEffect();
    });
  }

  // Custom background image file upload
  if (bgBtnCustomImage && customBgInput) {
    bgBtnCustomImage.addEventListener('click', () => {
      customBgInput.click();
    });

    customBgInput.addEventListener('change', () => {
      if (customBgInput.files && customBgInput.files[0]) {
        const file = customBgInput.files[0];
        if (customBgObjectUrl) URL.revokeObjectURL(customBgObjectUrl);
        customBgObjectUrl = URL.createObjectURL(file);

        const img = new Image();
        img.onload = () => {
          customBgImageObj = img;
          if (customBgPreviewImg) {
            customBgPreviewImg.src = customBgObjectUrl!;
            customBgPreviewImg.classList.remove('hidden');
          }
          if (photoPreviewWrapper && photoPreviewThumb && photoPreviewName) {
            photoPreviewThumb.src = customBgObjectUrl!;
            photoPreviewName.textContent = file.name;
            photoPreviewWrapper.classList.remove('hidden');
          }
          applyBgToElements(currentBgColor);
          applyBlurEffect();
          applyShadowEffect();
        };
        img.src = customBgObjectUrl;
      }
    });
  }

  if (photoRemoveBtn) {
    photoRemoveBtn.addEventListener('click', () => {
      clearCustomBg();
      applyBgToElements('transparent');
    });
  }

  function showToast(text: string) {
    if (!pasteToast) return;
    pasteToastText.textContent = text;
    pasteToast.classList.remove('translate-y-12', 'opacity-0', 'pointer-events-none');
    pasteToast.classList.add('translate-y-0', 'opacity-100');
    setTimeout(() => {
      pasteToast.classList.add('translate-y-12', 'opacity-0', 'pointer-events-none');
      pasteToast.classList.remove('translate-y-0', 'opacity-100');
    }, 2500);
  }

  function showError(msg: string) {
    if (errorBox && errorMessage) {
      errorMessage.textContent = msg;
      errorBox.classList.remove('hidden');
    }
  }

  function clearError() {
    if (errorBox) {
      errorBox.classList.add('hidden');
    }
  }

  if (closeErrorBtn) {
    closeErrorBtn.addEventListener('click', clearError);
  }

  function switchView(view: 'upload' | 'processing' | 'result') {
    clearError();
    uploadView.classList.add('hidden');
    processingView.classList.add('hidden');
    resultView.classList.add('hidden');

    if (view === 'upload') {
      uploadView.classList.remove('hidden');
    } else if (view === 'processing') {
      processingView.classList.remove('hidden');
    } else if (view === 'result') {
      resultView.classList.remove('hidden');
    }
  }

  function setSliderPercentage(pct: number) {
    const clamped = Math.max(0, Math.min(100, pct));
    if (stageBox) {
      stageBox.style.setProperty('--slider-pos', `${clamped.toFixed(2)}%`);
    }
  }

  function resetAll() {
    isCancelled = true;
    currentFile = null;
    clearCustomBg();
    currentBlur = 0;
    currentRatio = 'original';
    setCanvasRatio('original');
    setSliderPercentage(50);

    // Reset Blur Effect
    isBlurBgActive = false;
    blurBgPx = 0;
    if (blurBgToggle) blurBgToggle.checked = false;
    if (blurBgSlider) {
      blurBgSlider.value = '0';
      blurBgSlider.disabled = true;
      blurBgSlider.classList.add('opacity-50', 'disabled:cursor-not-allowed');
    }
    if (blurBgLabel) blurBgLabel.textContent = '0px';
    if (blurredOriginalBgImg) {
      blurredOriginalBgImg.classList.add('hidden');
      blurredOriginalBgImg.style.filter = 'none';
      blurredOriginalBgImg.src = '';
    }

    // Reset Shadow Effect
    isShadowActive = false;
    shadowOpacityPct = 0;
    if (shadowToggle) shadowToggle.checked = false;
    if (shadowSlider) {
      shadowSlider.value = '0';
      shadowSlider.disabled = true;
      shadowSlider.classList.add('opacity-50', 'disabled:cursor-not-allowed');
    }
    if (shadowLabel) shadowLabel.textContent = '0%';
    if (previewCutoutImg) {
      previewCutoutImg.style.filter = 'none';
    }

    originalImageObj = null;

    if (originalImageUrl) {
      URL.revokeObjectURL(originalImageUrl);
      originalImageUrl = null;
    }
    if (cutoutUrl) {
      URL.revokeObjectURL(cutoutUrl);
      cutoutUrl = null;
    }
    cutoutBlob = null;
    clearMagicStatus();
    if (magicPromptInput) magicPromptInput.value = '';
    switchBgTab('color');
    fileInput.value = '';
    switchView('upload');
  }

  if (uploadAnotherBtn) uploadAnotherBtn.addEventListener('click', resetAll);
  if (cancelProcessingBtn) cancelProcessingBtn.addEventListener('click', resetAll);

  // Background toggle clicks
  if (bgBtnTransparent) {
    bgBtnTransparent.addEventListener('click', () => {
      currentBgColor = 'transparent';
      clearCustomBg();
      applyBgToElements(currentBgColor);
    });
  }

  if (bgBtnWhite) {
    bgBtnWhite.addEventListener('click', () => {
      currentBgColor = '#FFFFFF';
      clearCustomBg();
      applyBgToElements(currentBgColor);
    });
  }

  if (bgBtnBlue) {
    bgBtnBlue.addEventListener('click', () => {
      currentBgColor = '#2563EB';
      clearCustomBg();
      applyBgToElements(currentBgColor);
    });
  }

  if (customColorPicker) {
    customColorPicker.addEventListener('input', (e) => {
      const val = (e.target as HTMLInputElement).value;
      currentBgColor = val;
      clearCustomBg();
      applyBgToElements(currentBgColor);
    });
  }

  // Backdrop Tab Switching Handler
  function switchBgTab(tab: 'color' | 'photo' | 'magic') {
    currentBgTab = tab;
    const activeClasses = ['active', 'bg-white', 'dark:bg-[#242428]', 'text-[#b47818]', 'dark:text-[#E5A93C]', 'font-semibold', 'shadow-xs', 'border-[#E5A93C]/30'];
    const inactiveClasses = ['text-[#5a5852]', 'dark:text-[#a09c92]', 'border-transparent'];

    const tabs = [
      { id: 'color', btn: bgTabBtnColor, content: bgTabContentColor },
      { id: 'photo', btn: bgTabBtnPhoto, content: bgTabContentPhoto },
      { id: 'magic', btn: bgTabBtnMagic, content: bgTabContentMagic },
    ];

    tabs.forEach(({ id, btn, content }) => {
      if (!btn || !content) return;
      if (id === tab) {
        btn.classList.add(...activeClasses);
        btn.classList.remove(...inactiveClasses);
        content.classList.remove('hidden');
      } else {
        btn.classList.remove(...activeClasses);
        btn.classList.add(...inactiveClasses);
        content.classList.add('hidden');
      }
    });
  }

  bgTabBtns.forEach(btn => {
    btn.addEventListener('click', () => {
      const tab = btn.getAttribute('data-tab') as 'color' | 'photo' | 'magic';
      if (tab) switchBgTab(tab);
    });
  });

  // Magic AI Background Generator Handlers
  function setMagicLoading(isLoading: boolean) {
    if (!magicGenerateBtn) return;
    magicGenerateBtn.disabled = isLoading;
    if (magicPromptInput) magicPromptInput.disabled = isLoading;

    if (isLoading) {
      if (magicBtnSpinner) magicBtnSpinner.classList.remove('hidden');
      if (magicBtnIcon) magicBtnIcon.classList.add('hidden');
      if (magicBtnText) magicBtnText.textContent = 'Generating...';
      magicGenerateBtn.classList.add('opacity-80', 'cursor-wait');
    } else {
      if (magicBtnSpinner) magicBtnSpinner.classList.add('hidden');
      if (magicBtnIcon) magicBtnIcon.classList.remove('hidden');
      if (magicBtnText) magicBtnText.textContent = 'Generate';
      magicGenerateBtn.classList.remove('opacity-80', 'cursor-wait');
    }
  }

  function showMagicStatus(msg: string, type: 'info' | 'error' | 'success') {
    if (!magicStatusBox || !magicStatusText) return;
    magicStatusText.textContent = msg;
    magicStatusBox.className = 'p-2.5 rounded-md text-xs font-medium flex items-center justify-between transition-all';

    if (type === 'error') {
      magicStatusBox.classList.add('bg-red-50', 'dark:bg-red-950/40', 'border', 'border-red-200', 'dark:border-red-800/60', 'text-red-700', 'dark:text-red-200');
    } else if (type === 'success') {
      magicStatusBox.classList.add('bg-emerald-50', 'dark:bg-emerald-950/40', 'border', 'border-emerald-200', 'dark:border-emerald-800/60', 'text-emerald-700', 'dark:text-emerald-300');
    } else {
      magicStatusBox.classList.add('bg-[#E5A93C]/10', 'border', 'border-[#E5A93C]/30', 'text-[#b47818]', 'dark:text-[#E5A93C]');
    }
  }

  function clearMagicStatus() {
    if (magicStatusBox) magicStatusBox.classList.add('hidden');
  }

  if (magicStatusClose) {
    magicStatusClose.addEventListener('click', clearMagicStatus);
  }

  async function generateMagicBackground() {
    if (!magicPromptInput || !magicGenerateBtn) return;
    const prompt = magicPromptInput.value.trim();
    if (!prompt) {
      showMagicStatus('Please enter a description for your background.', 'error');
      magicPromptInput.focus();
      return;
    }

    try {
      setMagicLoading(true);
      showMagicStatus('✨ Generating background with Flux AI... (~3-8s)', 'info');

      const res = await fetch('/api/generate-bg', {
        method: 'POST',
        headers: {
          'Content-Type': 'application/json',
        },
        body: JSON.stringify({ prompt }),
      });

      const data = await res.json().catch(() => null);

      if (!res.ok) {
        throw new Error(data?.error || `Generation failed with status ${res.status}`);
      }

      const generatedImage = data?.image || data?.imageUrl;
      if (!generatedImage) {
        throw new Error('No image returned from server.');
      }

      const img = new Image();
      await new Promise<void>((resolve, reject) => {
        img.onload = () => resolve();
        img.onerror = () => reject(new Error('Failed to render generated image asset'));
        img.src = generatedImage;
      });

      // Assign to existing background layer state without touching foreground/export logic
      customBgImageObj = img;
      if (customBgPreviewImg) {
        customBgPreviewImg.src = generatedImage;
        customBgPreviewImg.classList.remove('hidden');
      }

      // Update backdrop elements & apply active effects seamlessly
      currentBgColor = 'transparent';
      applyBgToElements(currentBgColor);
      applyBlurEffect();
      applyShadowEffect();

      showMagicStatus('✨ AI background generated and applied successfully!', 'success');
      showToast('✨ AI background applied');

    } catch (err: any) {
      console.error('Magic AI generation error:', err);
      showMagicStatus(err.message || 'Failed to generate background.', 'error');
    } finally {
      setMagicLoading(false);
    }
  }

  if (magicGenerateBtn) {
    magicGenerateBtn.addEventListener('click', generateMagicBackground);
  }

  if (magicPromptInput) {
    magicPromptInput.addEventListener('keydown', (e) => {
      if (e.key === 'Enter') {
        e.preventDefault();
        generateMagicBackground();
      }
    });
  }

  magicChips.forEach(btn => {
    btn.addEventListener('click', () => {
      const prompt = btn.getAttribute('data-prompt') || btn.textContent?.trim() || '';
      if (magicPromptInput) {
        magicPromptInput.value = prompt;
        magicPromptInput.focus();
      }
    });
  });

  // 60FPS Interactive Before/After Split Slider Drag Logic (Pointer Events API)
  let isDraggingSlider = false;
  let pendingSliderPos: number | null = null;
  let rafId: number | null = null;

  function updateSliderFromClientX(clientX: number) {
    if (!stageBox) return;
    const rect = stageBox.getBoundingClientRect();
    if (rect.width <= 0) return;
    const rawPct = ((clientX - rect.left) / rect.width) * 100;
    pendingSliderPos = Math.max(0, Math.min(100, rawPct));

    if (rafId === null) {
      rafId = requestAnimationFrame(() => {
        if (pendingSliderPos !== null && stageBox) {
          stageBox.style.setProperty('--slider-pos', `${pendingSliderPos.toFixed(2)}%`);
        }
        rafId = null;
      });
    }
  }

  function onPointerDown(e: PointerEvent) {
    isDraggingSlider = true;
    try {
      (e.currentTarget as HTMLElement)?.setPointerCapture?.(e.pointerId);
    } catch (_) {}
    updateSliderFromClientX(e.clientX);
  }

  function onPointerMove(e: PointerEvent) {
    if (!isDraggingSlider) return;
    updateSliderFromClientX(e.clientX);
  }

  function onPointerUp(e: PointerEvent) {
    if (isDraggingSlider) {
      isDraggingSlider = false;
      try {
        (e.currentTarget as HTMLElement)?.releasePointerCapture?.(e.pointerId);
      } catch (_) {}
    }
  }

  // Touch Event Handlers for Thumb Dragging on Mobile Devices
  function onTouchStart(e: TouchEvent) {
    if (e.touches && e.touches.length > 0) {
      isDraggingSlider = true;
      updateSliderFromClientX(e.touches[0].clientX);
    }
  }

  function onTouchMove(e: TouchEvent) {
    if (!isDraggingSlider) return;
    if (e.touches && e.touches.length > 0) {
      if (e.cancelable) e.preventDefault();
      updateSliderFromClientX(e.touches[0].clientX);
    }
  }

  function onTouchEnd() {
    if (isDraggingSlider) {
      isDraggingSlider = false;
    }
  }

  // Mouse fallback handlers
  function onMouseDown(e: MouseEvent) {
    isDraggingSlider = true;
    updateSliderFromClientX(e.clientX);
  }

  function onMouseMove(e: MouseEvent) {
    if (!isDraggingSlider) return;
    updateSliderFromClientX(e.clientX);
  }

  function onMouseUp() {
    if (isDraggingSlider) isDraggingSlider = false;
  }

  if (stageBox) {
    // Pointer Events API
    stageBox.addEventListener('pointerdown', onPointerDown);
    stageBox.addEventListener('pointermove', onPointerMove);
    stageBox.addEventListener('pointerup', onPointerUp);
    stageBox.addEventListener('pointercancel', onPointerUp);

    // Explicit Touch Events with passive: false for smooth non-scrolling thumb drag
    stageBox.addEventListener('touchstart', onTouchStart, { passive: false });
    stageBox.addEventListener('touchmove', onTouchMove, { passive: false });
    stageBox.addEventListener('touchend', onTouchEnd);
    stageBox.addEventListener('touchcancel', onTouchEnd);

    // Mouse fallback
    stageBox.addEventListener('mousedown', onMouseDown);
  }

  // Global window listeners for drag continuation outside container
  window.addEventListener('pointermove', (e: PointerEvent) => {
    if (isDraggingSlider) updateSliderFromClientX(e.clientX);
  });
  window.addEventListener('pointerup', () => {
    if (isDraggingSlider) isDraggingSlider = false;
  });
  window.addEventListener('pointercancel', () => {
    if (isDraggingSlider) isDraggingSlider = false;
  });

  window.addEventListener('touchmove', (e: TouchEvent) => {
    if (isDraggingSlider && e.touches && e.touches.length > 0) {
      if (e.cancelable) e.preventDefault();
      updateSliderFromClientX(e.touches[0].clientX);
    }
  }, { passive: false });
  window.addEventListener('touchend', onTouchEnd);
  window.addEventListener('touchcancel', onTouchEnd);

  window.addEventListener('mousemove', onMouseMove);
  window.addEventListener('mouseup', onMouseUp);

  if (resetSliderBtn) {
    resetSliderBtn.addEventListener('click', (e) => {
      e.stopPropagation();
      setSliderPercentage(50);
    });
  }

  // Core Processing Routine
  async function processImage(file: File | Blob) {
    try {
      isCancelled = false;
      currentFile = file;
      clearError();

      if (file.size > 25 * 1024 * 1024) {
        showError('Image exceeds 25MB limit. Please choose a smaller file.');
        return;
      }

      if (originalImageUrl) URL.revokeObjectURL(originalImageUrl);
      originalImageUrl = URL.createObjectURL(file);
      previewOriginalImg.src = originalImageUrl;
      if (blurredOriginalBgImg) {
        blurredOriginalBgImg.src = originalImageUrl;
      }

      const imgProbe = new Image();
      imgProbe.src = originalImageUrl;
      await new Promise((resolve) => {
        imgProbe.onload = () => {
          imageNaturalWidth = imgProbe.naturalWidth;
          imageNaturalHeight = imgProbe.naturalHeight;
          originalImageObj = imgProbe;
          resolve(null);
        };
        imgProbe.onerror = () => resolve(null);
      });

      switchView('processing');
      progressBarFill.style.width = '15%';
      processingTitle.textContent = 'Initializing in-browser neural engine...';
      processingSubtitle.textContent = 'Loading WebAssembly runtime...';

      const { removeBackground } = await import('@imgly/background-removal');

      if (isCancelled) return;

      progressBarFill.style.width = '35%';
      processingTitle.textContent = 'Segmenting foreground...';
      processingSubtitle.textContent = 'Calculating pixel contours in browser RAM...';

      const rawBlob = await removeBackground(file, {
        progress: (key: string, current: number, total: number) => {
          if (isCancelled) return;
          if (total > 0) {
            const pct = Math.min(94, Math.round((current / total) * 100));
            progressBarFill.style.width = `${Math.max(35, pct)}%`;
          }
          if (key.includes('fetch')) {
            processingSubtitle.textContent = 'Caching model weights locally in browser (~20MB)...';
          } else if (key.includes('compute')) {
            processingSubtitle.textContent = 'Executing inference via WebAssembly...';
          }
        },
        model: 'medium',
        output: {
          format: 'image/png',
          quality: 1.0,
          type: 'foreground'
        }
      });

      if (isCancelled) return;

      progressBarFill.style.width = '97%';
      processingTitle.textContent = 'Refining alpha contours & edge feathering...';
      processingSubtitle.textContent = 'Applying PhotoRoom-grade trimap thresholding & perimeter erosion...';

      const resultBlob = await refineAlphaMask(rawBlob, file);

      if (isCancelled) return;

      progressBarFill.style.width = '100%';
      processingTitle.textContent = 'Compositing full HD PNG...';

      cutoutBlob = resultBlob;
      if (cutoutUrl) URL.revokeObjectURL(cutoutUrl);
      cutoutUrl = URL.createObjectURL(resultBlob);

      previewCutoutImg.src = cutoutUrl;

      updateResolutionBadge();

      const mbSize = (resultBlob.size / (1024 * 1024)).toFixed(2);
      metaSizeBadge.textContent = `${mbSize} MB PNG`;

      applyBgToElements(currentBgColor);
      applyBlurEffect();
      applyShadowEffect();
      setSliderPercentage(50);

      setTimeout(() => {
        if (!isCancelled) {
          switchView('result');
        }
      }, 200);

    } catch (err: any) {
      console.error('Background removal error:', err);
      if (!isCancelled) {
        switchView('upload');
        showError(err?.message || 'Processing failed. Please check your image format.');
      }
    }
  }

  if (dropZone && fileInput) {
    dropZone.addEventListener('click', () => fileInput.click());
    dropZone.addEventListener('keydown', (e) => {
      if (e.key === 'Enter' || e.key === ' ') {
        e.preventDefault();
        fileInput.click();
      }
    });

    ['dragenter', 'dragover'].forEach(eventName => {
      dropZone.addEventListener(eventName, (e) => {
        e.preventDefault();
        e.stopPropagation();
        dropZone.classList.add('border-[#E5A93C]', 'bg-[#E5A93C]/5', 'dark:bg-[#E5A93C]/10', 'shadow-[0_0_20px_rgba(229,169,60,0.2)]');
      }, false);
    });

    ['dragleave', 'drop'].forEach(eventName => {
      dropZone.addEventListener(eventName, (e) => {
        e.preventDefault();
        e.stopPropagation();
        dropZone.classList.remove('border-[#E5A93C]', 'bg-[#E5A93C]/5', 'dark:bg-[#E5A93C]/10', 'shadow-[0_0_20px_rgba(229,169,60,0.2)]');
      }, false);
    });

    dropZone.addEventListener('drop', (e) => {
      const dt = e.dataTransfer;
      if (dt && dt.files && dt.files[0]) {
        const file = dt.files[0];
        if (file.type.startsWith('image/')) {
          processImage(file);
        } else {
          showError('Please drop a valid image (PNG, JPG, or WebP).');
        }
      }
    });
  }

  if (fileInput) {
    fileInput.addEventListener('change', () => {
      if (fileInput.files && fileInput.files[0]) {
        processImage(fileInput.files[0]);
      }
    });
  }

  window.addEventListener('paste', (e) => {
    const clipboardData = e.clipboardData;
    if (!clipboardData) return;

    const items = clipboardData.items;
    for (let i = 0; i < items.length; i++) {
      if (items[i].type.indexOf('image') !== -1) {
        const file = items[i].getAsFile();
        if (file) {
          showToast('Image pasted from clipboard');
          processImage(file);
          break;
        }
      }
    }
  });

  const sampleButtons = document.querySelectorAll('.sample-btn');
  sampleButtons.forEach(btn => {
    btn.addEventListener('click', async () => {
      const sampleUrl = btn.getAttribute('data-sample');
      if (!sampleUrl) return;

      try {
        switchView('processing');
        if (processingTitle) processingTitle.textContent = 'Loading sample image...';
        if (progressBarFill) progressBarFill.style.width = '10%';

        const response = await fetch(sampleUrl);
        if (!response.ok) throw new Error('Could not load sample');
        const blob = await response.blob();
        await processImage(blob);
      } catch (err: any) {
        switchView('upload');
        showError('Failed to load sample: ' + err.message);
      }
    });
  });

  // Primary Export: Download Full HD PNG
  if (downloadHdBtn) {
    downloadHdBtn.addEventListener('click', async () => {
      if (!cutoutBlob) return;
      try {
        downloadHdBtn.disabled = true;
        downloadHdBtn.classList.add('opacity-75');

        const originalName = currentFile instanceof File ? currentFile.name.replace(/\.[^/.]+$/, "") : 'cutout';
        const filename = `${originalName}-fastbgremove-hd.png`;

        const exportOpts: ExportOptions = {
          ratio: currentRatio,
          backgroundColor: currentBgColor,
          customBgImage: customBgImageObj,
          originalImage: originalImageObj,
          blur: blurBgPx,
          isBlurEnabled: isBlurBgActive,
          isShadowEnabled: isShadowActive,
          shadowOpacity: shadowOpacityPct
        };

        const finalBlob = await createHdExportBlob(cutoutBlob, exportOpts);
        triggerDownload(finalBlob, filename);
        showToast('Download started');
      } catch (err) {
        console.error('Export download error:', err);
        showError('Failed to export HD PNG.');
      } finally {
        downloadHdBtn.disabled = false;
        downloadHdBtn.classList.remove('opacity-75');
      }
    });
  }

  // Secondary Export: Export Compressed JPG (Binary Search)
  if (exportJpgBtn) {
    exportJpgBtn.addEventListener('click', async () => {
      if (!cutoutBlob) return;
      try {
        exportJpgBtn.disabled = true;
        const originalBtnText = exportJpgText ? exportJpgText.textContent : 'Export JPG';
        if (exportJpgText) exportJpgText.textContent = 'Optimizing...';

        const preset = ((jpgPresetSelect ? jpgPresetSelect.value : '100kb') as JpgCompressionPreset) || '100kb';
        const exportOpts: ExportOptions = {
          ratio: currentRatio,
          backgroundColor: currentBgColor,
          customBgImage: customBgImageObj,
          originalImage: originalImageObj,
          blur: blurBgPx,
          isBlurEnabled: isBlurBgActive,
          isShadowEnabled: isShadowActive,
          shadowOpacity: shadowOpacityPct
        };

        const finalBlob = await createCompressedJpgBlob(cutoutBlob, preset, exportOpts);
        const originalName = currentFile instanceof File ? currentFile.name.replace(/\.[^/.]+$/, "") : 'cutout';
        const filename = `${originalName}-fastbgremove-${preset}.jpg`;

        triggerDownload(finalBlob, filename);
        const sizeKb = (finalBlob.size / 1024).toFixed(1);
        showToast(`JPG downloaded (${sizeKb} KB)`);
        if (exportJpgText) exportJpgText.textContent = originalBtnText;
      } catch (err) {
        console.error('JPG export error:', err);
        showError('Failed to export compressed JPG.');
        if (exportJpgText) exportJpgText.textContent = 'Export JPG';
      } finally {
        exportJpgBtn.disabled = false;
      }
    });
  }

  // Copy to Clipboard
  if (copyClipboardBtn) {
    copyClipboardBtn.addEventListener('click', async () => {
      if (!cutoutBlob) return;
      try {
        copyClipboardBtn.disabled = true;
        const originalText = copyBtnText ? copyBtnText.textContent : 'Copy PNG';
        if (copyBtnText) copyBtnText.textContent = 'Copying...';

        const exportOpts: ExportOptions = {
          ratio: currentRatio,
          backgroundColor: currentBgColor,
          customBgImage: customBgImageObj,
          originalImage: originalImageObj,
          blur: blurBgPx,
          isBlurEnabled: isBlurBgActive,
          isShadowEnabled: isShadowActive,
          shadowOpacity: shadowOpacityPct
        };

        const finalBlob = await createHdExportBlob(cutoutBlob, exportOpts);
        const success = await copyImageToClipboard(finalBlob);

        if (success) {
          if (copyBtnText) copyBtnText.textContent = 'Copied!';
          showToast('Cutout copied to clipboard');
          setTimeout(() => {
            if (copyBtnText) copyBtnText.textContent = originalText;
            copyClipboardBtn.disabled = false;
          }, 2000);
        } else {
          if (copyBtnText) copyBtnText.textContent = 'Failed';
          showError('Clipboard copy is not supported in this browser.');
          setTimeout(() => {
            if (copyBtnText) copyBtnText.textContent = originalText;
            copyClipboardBtn.disabled = false;
          }, 2000);
        }
      } catch (err) {
        console.error('Clipboard copy error:', err);
        copyClipboardBtn.disabled = false;
      }
    });
  }
</script>
